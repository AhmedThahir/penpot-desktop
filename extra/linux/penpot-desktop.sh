# This should never be a fixed name, since the version number in the file name will change when an update occurs.
# By using "*", well it's a simple workaround. 
/usr/share/korbs-studio/penpot-desktop/app/Penpot*.AppImage --no-sandbox %U