---
kind: pipeline
type: exec
name: Build on macOS

clone:
  retries: 3

platform:
  os: darwin
  arch: arm64

steps:
- name: Build
  commands:
    - npm i
    - npm run build

    - mv dist/Penpot\ Desktop*.dmg dist/Penpot.dmg
    - mv dist/Penpot\ Desktop*.zip dist/Penpot.zip